import type { CustomFlowbiteTheme } from "flowbite-react";

const customSignUpTheme: CustomFlowbiteTheme["button"] = {
  color: {
    custom: "border border-transparent bg-green-primary text-white focus:ring-4 focus:ring-green-300 enabled:hover:bg-green-darker enabled:hover:text-white dark:bg-green-600 dark:focus:ring-green-800 dark:enabled:hover:bg-green-700"
  }
};

export default customSignUpTheme;