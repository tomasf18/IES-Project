import type { CustomFlowbiteTheme } from "flowbite-react";

const customLoginButtonTheme: CustomFlowbiteTheme["button"] = {
  color: {
    custom: "border border-transparent bg-gray-400 text-white focus:ring-4 focus:ring-gray-300 enabled:hover:bg-gray-500 enabled:hover:text-white dark:bg-green-600 dark:focus:ring-green-800 dark:enabled:hover:bg-green-700"
  }
};

export default customLoginButtonTheme;